export const metadata = {
  title: "Login | Nexuspace",
  description: "Sign in to access your Nexuspace workspaces.",
  alternates: {
    canonical: "https://project-nexuspace.vercel.app/login"
  }
};

export default function LoginLayout({ children }) {
  return children;
}

